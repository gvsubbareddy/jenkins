pipeline {
  agent any
  stages {
    stage('testing pipeline') {
      steps {
        echo 'test1'
      }
    }
    stage('build') {
      steps {
        echo 'started the build'
      }
    }
  }
}